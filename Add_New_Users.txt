python

from database import Session, User
session = Session()
# Add yourself as an admin
new_admin = User(name="Somar", employee_id="SK965050", is_admin=True)
session.add(new_admin)
session.commit()
print("✅ Admin user added successfully!")
session.close()